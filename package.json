{
    "name": "vanilla-jsx",
    "version": "2.0.0",
    "private": true,
    "scripts": {
        "build": "tsc",
        "build:test": "tsc -p ./test/tsconfig.json"
    },
    "devDependencies": {
        "typescript": "^5.8.3"
    }
}
